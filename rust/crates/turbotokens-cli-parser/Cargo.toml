[package]
name = "turbotokens-cli-parser"
version.workspace = true
edition.workspace = true
build = "build.rs"
publish.workspace = true

[dependencies]
turbotokens-cli.workspace = true

[build-dependencies]
serde_json.workspace = true

[dev-dependencies]
# The config-merge tests parse a command line against a config file, so they
# need the config layer and a filesystem fixture.
turbotokens-config.workspace = true
turbotokens-core.workspace = true
turbotokens-test-support.workspace = true
insta.workspace = true
serde_json.workspace = true
