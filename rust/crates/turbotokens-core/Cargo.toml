[package]
name = "turbotokens-core"
version.workspace = true
edition.workspace = true
build = "build.rs"
publish.workspace = true

[features]
# Off by default: the pricing snapshot normally arrives through
# TURBOTOKENS_PRICING_JSON_PATH, and minreq's rustls stack is the workspace's most
# expensive build-dependency. See build.rs.
fetch-litellm-pricing = ["dep:minreq"]

[dependencies]
turbotokens-cli.workspace = true
turbotokens-terminal.workspace = true
jiff.workspace = true
memchr.workspace = true
rustc-hash.workspace = true
serde.workspace = true
serde_json.workspace = true
smallvec.workspace = true

[build-dependencies]
minreq = { workspace = true, optional = true }
serde_json.workspace = true

[dev-dependencies]
turbotokens-test-support.workspace = true
insta.workspace = true
